const name = require('./name')
const token = require('./token')
const authNickname = require('./authNickname')
const authTypeSelect = require('./authTypeSelect')
const usernameAndPassword = require('./usernameAndPassword')

module.exports = {
  name,
  token,
  authNickname,
  authTypeSelect,
  usernameAndPassword
}
